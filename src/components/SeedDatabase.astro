---
// src/components/SeedDatabase.astro

// ข้อมูลเมล็ดพันธุ์และวัสดุทั้งหมด (จัดหมวดหมู่ตามที่คุณให้มา)
const seedCategories = [
  {
    title: "1. เมล็ดพืชทั่วไปและธัญพืช",
    // ใช้สีเขียวใบไม้
    colorClass: "bg-green-100 text-green-800 border-green-200",
    items: [
      "ถั่วเขียวแกะเปลือก", "ถั่วเขียว", "ถั่วเขียวเปลือกดำ", "ถั่ว (เม็ดใหญ่)",
      "ข้าวโพดหวาน", "ข้าวโพด", "ลูกเดือยสีขาว",
      "งาดำ", "งาดำกะเทาะเปลือก (สีขาว)", "งาขาว",
      "เมล็ดกาแฟ (มีเปลือก)", "กาแฟสาร",
      "ปอเทือง", "ปอ",
      "ข้าวเปลือก กข6 (ข้าวเหนียว)", "ข้าวเปลือกหอมมะลิ", 
      "ข้าวสุพรรณ 35", "ข้าวสุพรรณ 1", "ข้าวชัยนาท",
      "ข้าวสารหอมมะลิ", "ข้าวหอมมะลิ",
      "เม็ดดอกดาวเรือง", "เม็ดทานตะวันเม็ดเล็ก", "เม็ดทานตะวันเม็ดใหญ่",
      "ขนไก่ป่น"
    ]
  },
  {
    title: "2. เมล็ดพันธุ์ผักและผลไม้",
    // ใช้สีเขียวมรกต
    colorClass: "bg-emerald-100 text-emerald-800 border-emerald-200",
    items: [
      "มะเขือเทศเชอรี่", "มะเขือเทศ", "มะเขือเปราะ",
      "พริกประดิบ", "พริก", "เมล็ดพริก",
      "บวบ", "เมล็ดบวบพิมพา",
      "สร้อยไก่", "น้ำเต้า", "ชุมเห็ด", "ผักปลัง",
      "แตงโมจินตหรา (เม็ดลาย)", "เมล็ดแตงโม (sugar baby)", "เมล็ดแตงโม จินตหรา",
      "เมล็ดแคนตาลูป", "เมล็ดแฟง",
      "เมล็ดถั่วฝักยาว",
      "เมล็ดผักกาด", "เมล็ดผักกาดเขียวปลี", "เมล็ดผักกาดกวางตุ้ง", "เมล็ดผักกาดขาวปลี",
      "เมล็ดกะหล่ำดอก", "เมล็ดกะหล่ำปลี",
      "เมล็ดแครอทพิวดูโรท", 
      "เมล็ดฟักทองพิวดูโรท", "เมล็ดฟักทอง (มีเปลือก)",
      "มะระไทสัน", "เมล็ดผักบุ้ง",
      "เมล็ดคะน้า (super k)", "เมล็ดผักคะน้า",
      "เมล็ดแตงกวา"
    ]
  },
  {
    title: "3. วัสดุเม็ดหรือผงอื่นๆ",
    // ใช้สีเหลือง/ส้ม
    colorClass: "bg-orange-100 text-orange-800 border-orange-200",
    items: [
      "รำข้าวป่น", "รำละเอียด", "รำสกัด",
      "ปลาป่น", "ปลาข้าวป่น",
      "ข่าป่น", "ขมิ้นอ้อย (ผง)", "พญาว่าน (ผง)", "มะขาม (ผง)", "ไพร (ผง)", "พริกป่น",
      "ข้าวเกรียบ", "ผงไม้", "ดินสอพอง (ผง)",
      "มันสำปะหลัง (ผง)", "มันสำปะหลังป่น", "กากมัน",
      "กากถั่วเหลือง", "ถั่วเหลืองป่น", "พูลแฟตซอย (ผง)",
      "ข้าวโพดป่น", "อาหารสัตว์มีข้าวโพดเป็นส่วนประกอบหลัก",
      "อาหารสัตว์ (ผง)", "อาหารหมา", "อาหารกุ้ง",
      "สาเหล้า (เกล็ด)", "กากเบียร์ (ผง)",
      "ปุ๋ยขี้เป็ด", "เมล็ดทานตะวันป่น"
    ]
  },
  {
    title: "4. กลุ่มพริกไทย",
    // ใช้สีเทา/น้ำตาล
    colorClass: "bg-stone-100 text-stone-800 border-stone-200",
    items: [
      "พริกไทยเม็ดสีน้ำตาล", "พริกไทยขาวป่น (A)", 
      "พริกไทยดำผ่า 3 ซีก", "พริกไทยดำป่น",
      "พริกไทยผง (60เมส)", "พริกไทยผง (20เมส)",
      "พริกไทยขาวกะเทาะเปลือก (เม็ดเล็ก) สีน้ำตาล",
      "พริกไทยเม็ดสีขาว (สีเทา)", "พริกไทยขาว (สีเทา)",
      "พริกไทยป่น"
    ]
  }
];
---

<section class="py-12 bg-white" id="seed-database">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <div class="text-center mb-10">
      <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        ฐานข้อมูลวัสดุที่รองรับ
      </h2>
      <p class="mt-4 text-lg text-gray-500 max-w-2xl mx-auto">
        รายชื่อเมล็ดพันธุ์และวัสดุที่เราได้ทำการทดสอบมาตรฐาน (Standard Calibration) เรียบร้อยแล้ว พร้อมสำหรับการผลิตเครื่องวัดให้คุณทันที
      </p>
      
      <div class="mt-8 max-w-md mx-auto relative group">
        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
          <svg class="h-5 w-5 text-gray-400 group-focus-within:text-blue-500 transition-colors" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </div>
        <input 
          type="text" 
          id="seedSearch" 
          class="block w-full pl-10 pr-3 py-3 border border-gray-300 rounded-lg leading-5 bg-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-shadow sm:text-sm shadow-sm" 
          placeholder="ค้นหาชื่อเมล็ดพืช (เช่น ข้าว, พริกไทย)..."
        >
      </div>
    </div>

    <div class="space-y-12 min-h-[300px]">
      {seedCategories.map((category) => (
        <div class="category-section animate-fade-in" data-category={category.title}>
          
          <div class="flex items-center space-x-3 mb-5 border-b pb-2 border-gray-100">
            <span class={`px-3 py-1 rounded-full text-sm font-semibold border ${category.colorClass}`}>
               หมวดหมู่
            </span>
            <h3 class="text-xl font-bold text-gray-800">{category.title}</h3>
          </div>
          
          <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            {category.items.map((item) => (
              <div class="seed-item bg-gray-50 rounded-lg p-3 text-gray-700 hover:bg-blue-50 hover:text-blue-700 transition-all duration-200 border border-transparent hover:border-blue-200 hover:shadow-sm cursor-default flex items-center">
                <span class="text-gray-400 mr-2">•</span> {item}
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <div id="noResults" class="hidden text-center py-10">
      <div class="bg-yellow-50 rounded-xl p-8 max-w-lg mx-auto border border-yellow-200 shadow-sm">
        <div class="text-5xl mb-4">🤔</div>
        <h3 class="text-lg font-bold text-gray-900 mb-2">ไม่พบรายการที่คุณค้นหา</h3>
        <p class="text-gray-600 mb-6">
          ลองใช้คำค้นหาอื่น หรือดูรายการทั้งหมดอีกครั้ง
        </p>
        
        <button 
          id="showAllBtn"
          class="inline-flex items-center px-5 py-2.5 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-all shadow-md hover:shadow-lg"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
          </svg>
          ล้างคำค้นหา / ดูทั้งหมด
        </button>

        <div class="mt-6 pt-6 border-t border-yellow-200">
           <p class="text-sm text-gray-500">
            หากเป็นวัสดุชนิดใหม่ที่ไม่มีในรายการ <br>โปรดติดต่อเราเพื่อส่งตัวอย่างทดสอบ
          </p>
        </div>
      </div>
    </div>

  </div>
</section>

<script>
  // Script สำหรับระบบค้นหา (Search Logic)
  const searchInput = document.getElementById('seedSearch');
  const showAllBtn = document.getElementById('showAllBtn');
  const categories = document.querySelectorAll('.category-section');
  const noResults = document.getElementById('noResults');

  // ฟังก์ชันกรองข้อมูล
  function filterItems() {
    const term = searchInput.value.toLowerCase().trim();
    let hasGlobalResults = false;

    categories.forEach((category) => {
      let hasCategoryResults = false;
      const categoryItems = category.querySelectorAll('.seed-item');

      categoryItems.forEach((item) => {
        const text = item.textContent.toLowerCase();
        // ค้นหาแบบ partial match (พิมพ์บางส่วนก็เจอ)
        if (text.includes(term)) {
          item.style.display = 'flex'; // แสดงรายการ (ใช้ flex เพื่อจัด icon หน้าข้อความ)
          hasCategoryResults = true;
          hasGlobalResults = true;
        } else {
          item.style.display = 'none'; // ซ่อนรายการ
        }
      });

      // ถ้าหมวดหมู่นั้นไม่มีรายการเหลือเลย ให้ซ่อนหัวข้อหมวดหมู่ไปด้วย
      if (hasCategoryResults) {
        category.style.display = 'block';
      } else {
        category.style.display = 'none';
      }
    });

    // ถ้าไม่เจออะไรเลยสักอย่างในทุกหมวดหมู่
    if (!hasGlobalResults && term !== '') {
      noResults.classList.remove('hidden');
    } else {
      noResults.classList.add('hidden');
    }
  }

  // ฟังก์ชันล้างค่า (Reset) เมื่อกดปุ่ม
  function resetSearch() {
    searchInput.value = ''; // ล้างช่องค้นหา
    filterItems(); // เรียกฟังก์ชันกรองใหม่ (ซึ่งจะแสดงทั้งหมดเพราะค่าว่าง)
    searchInput.focus(); // เอา cursor ไปวางที่ช่องค้นหาให้ลูกค้าพิมพ์ใหม่ได้เลย
  }

  // Event Listeners (รอรับการพิมพ์และการคลิก)
  if(searchInput && showAllBtn) {
    searchInput.addEventListener('input', filterItems);
    showAllBtn.addEventListener('click', resetSearch);
  }
</script>